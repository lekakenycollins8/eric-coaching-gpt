// This file has been commented out to prevent prerendering errors during build.
// If you need a custom error page, consider using a different approach or check Next.js documentation for updates.
/*
"use client";

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <html>
      <body>
        <div style={{ textAlign: 'center', padding: '50px' }}>
          <h2>Something went wrong!</h2>
          <p>There was an error in rendering the page: {error.message}</p>
          <button 
            onClick={() => reset()}
            style={{ padding: '10px 20px', cursor: 'pointer' }}
          >
            Try again
          </button>
        </div>
      </body>
    </html>
  );
}
*/
